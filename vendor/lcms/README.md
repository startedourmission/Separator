Vendored from `lcms-wasm@1.0.5` (MIT), https://github.com/mattdesl/lcms-wasm.
`lcms.js`, `lcms.wasm`, and `LICENSE.md` are unmodified npm distribution files.
Bundled here so the static site needs neither a CDN nor an npm build to use ICC transforms.
